apiVersion: v1
kind: Service
metadata:
  name: order-inventory-service
  labels:
    app: order-inventory-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: order-inventory-service